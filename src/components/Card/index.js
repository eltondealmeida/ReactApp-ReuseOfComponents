import React from 'react';
import './style.css';

export default function Card() {
 return (
  <div className="card-container">
   <p>Components:</p>
   <p>Makes it easier to keep smaller parts running smoothly</p>
   <p>You can reuse them, i.e., less code to write</p>
  </div>
 );
}